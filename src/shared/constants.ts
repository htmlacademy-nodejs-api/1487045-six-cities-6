export enum Rating {
  Min = 1,
  Max = 5,
}

export enum BedroomsAmount {
  Min = 1,
  Max = 8,
}

export enum GuestsAmount {
  Min = 1,
  Max = 10,
}

export enum PriceRange {
  Min = 100,
  Max = 100000,
}

export enum TitleLength {
  Min = 10,
  Max = 100,
}

export enum DescriptionLength {
  Min = 20,
  Max = 1024,
}

export enum CommentLength {
  Min = 5,
  Max = 1024,
}

export const IMAGE_URL_MAX_LENGTH = 256;

export enum UsernameLength {
  Min = 1,
  Max = 15,
}

export enum PasswordLength {
  Min = 6,
  Max = 12,
}
